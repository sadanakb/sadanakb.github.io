---
interface Props {
  name: string;
}

const { name } = Astro.props;
---

<span class="inline-block px-2.5 py-1 text-xs font-mono bg-surface border border-border rounded-full text-muted whitespace-nowrap hover:border-accent/30 hover:text-accent transition-all duration-200">
  {name}
</span>
